openapi: 3.0.3

info:
  title: Octopus Format OpenAPI specification
  version: 0.0.1
paths:
  /:
    get:
      responses:
        "200":
          description: OctopusDocument
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OctopusDocument'
components:
  schemas:
    $ref: './schemas.yaml'
