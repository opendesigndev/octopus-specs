description:
  Inner shadow effect description.

type: object

allOf:
  - $ref: '../schemas.yaml#/EffectBase'

  - type: object
    properties:
      shadow:
        $ref: '../schemas.yaml#/ShadowConfig'

    required: [shadow]
