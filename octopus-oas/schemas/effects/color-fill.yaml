description:
  Color fill effect.

type: object

allOf:
  - $ref: '../schemas.yaml#/EffectBase'

  - type: object
    properties:
      color:
        $ref: '../schemas.yaml#/Color'

    required: [color]
