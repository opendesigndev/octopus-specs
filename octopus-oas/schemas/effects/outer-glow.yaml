description:
  Outer glow effect description.

type: object

allOf:
  - $ref: '../schemas.yaml#/EffectBase'

  - type: object
    properties:
      glow:
        $ref: '../schemas.yaml#/ShadowConfig'

    required: [glow]
