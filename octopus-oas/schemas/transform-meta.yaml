description:
  Transformation source data

type: object

properties:
  rotation:
    description: rotation
    type: number
    format: float
    default: 0.0

  origin:
    $ref: './schemas.yaml#/Vec2'

  scale:
    $ref: './schemas.yaml#/Vec2'
    default: [1.0, 1.0]

  horizontal-flip:
    type: boolean
    default: false

  vertical-flip:
    type: boolean
    default: false
