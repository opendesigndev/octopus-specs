description: ID used to reference a document (i.e. artboard or component)
type: object

oneOf:
  - $ref: '../schemas.yaml#/ArtboardID'
  - $ref: '../schemas.yaml#/ComponentID'

discriminator:
  propertyName: type
  mapping:
    ARTBOARD: '#/components/schemas/ArtboardID'
    COMPONENT: '#/components/schemas/ComponentID'
