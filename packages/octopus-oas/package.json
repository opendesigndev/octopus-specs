{
  "name": "@avocode/octopus-oas",
  "version": "3.0.0-alpha.13",
  "files": [
    "dist/openapi.json"
  ],
  "scripts": {
    "build": "./scripts/assemble.sh",
    "release": "yarn publish --non-interactive",
    "version:bump": "yarn version --no-git-tag-version --new-version"
  },
  "devDependencies": {
    "@apidevtools/swagger-cli": "4.0.4"
  }
}
