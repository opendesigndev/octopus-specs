description:
  Color fill effect.

type: object

allOf:
  - $ref: '../schemas.yaml#/FillBase'

  - type: object
    properties:
      type:
        type: string
        enum:
          - COLOR

      color:
        $ref: '../schemas.yaml#/Color'

    required: [color]
