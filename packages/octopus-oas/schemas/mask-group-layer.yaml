description:
  Mask (TODO).

allOf:
  - $ref: './schemas.yaml#/LayerBase'

  - type: object
    properties:
      mask:
        $ref: './schemas.yaml#/Layer'

      layers:
        type: array
        items:
          $ref: './schemas.yaml#/Layer'

    required: [mask, layers]
