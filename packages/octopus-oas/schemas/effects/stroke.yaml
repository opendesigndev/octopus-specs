description:
  (Vector) stroke effect description.

type: object

allOf:
  - $ref: '../schemas.yaml#/EffectLikeBase'

  - type: object
    properties:
      stroke:
        $ref: '../schemas.yaml#/VectorStrokeConfig'

    required: [stroke]
