description:
  Inner glow effect description.

type: object

allOf:
  - $ref: '../schemas.yaml#/EffectBase'

  - type: object
    properties:
      type:
        type: string
        enum:
          - INNER_GLOW

      glow:
        $ref: '../schemas.yaml#/ShadowConfig'

    required: [glow]
