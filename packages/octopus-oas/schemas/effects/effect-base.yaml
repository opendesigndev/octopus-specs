description:
  Common data fields of all real effects (shadows, blurs...)

type: object

allOf:
  - $ref: '../schemas.yaml#/EffectBase'

  - type: object
    properties:
      type:
        $ref: '../schemas.yaml#/EffectType'

required: [type]
