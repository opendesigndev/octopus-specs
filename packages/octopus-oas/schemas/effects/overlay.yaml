description:
  Overlay effect description.

type: object

allOf:
  - $ref: '../schemas.yaml#/EffectBase'

  - type: object
    properties:
      overlay:
        $ref: '../schemas.yaml#/Fill'

    required: [overlay]
