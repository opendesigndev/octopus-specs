description:
  Common data fields of all effects.

type: object

properties:
  visible:
    type: boolean
    default: true
    description:
      Toggles whether the effect is visible.

  blendMode:
    description: Blend mode of the effect.
    default: NORMAL
    $ref: '../schemas.yaml#/BlendMode'
