description:
  Gaussian blur effect.

type: object

allOf:
  - $ref: '../schemas.yaml#/EffectBase'

  - type: object
    properties:
      blur:
        type: number
        format: float

    required: [blur]
