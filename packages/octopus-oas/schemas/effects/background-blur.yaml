description:
  Background blur effect.

type: object

allOf:
  - $ref: '../schemas.yaml#/EffectBase'

  - type: object
    properties:
      blur:
        type: number
        format: float

      filters:
        type: array
        items:
          $ref: '../schemas.yaml#/ImageFilter'

    required: [blur]
