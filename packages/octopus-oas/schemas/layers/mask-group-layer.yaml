description:
  Mask (TODO).

allOf:
  - $ref: '../schemas.yaml#/LayerBase'

  - type: object
    properties:
      mask:
        $ref: '../schemas.yaml#/Layer'

      maskBasis:
        description: Describes the part of a masking layer that in fact serves as the mask.
        $ref: '../schemas.yaml#/MaskBasis'

      layers:
        type: array
        items:
          $ref: '../schemas.yaml#/Layer'

    required: [mask, maskBasis, layers]
