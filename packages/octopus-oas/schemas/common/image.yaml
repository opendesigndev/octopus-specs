description: Image description.

type: object

properties:
  ref:
    $ref: '../schemas.yaml#/ImageRef'

  subsection:
    description: Selects a rectangular area from the referenced image. If missing, the image is selected.

    $ref: '../schemas.yaml#/Rectangle'

required: [ref]
